#var = input()
#if statement:
	#consequence==

#elif
#else
#uses keywords instead of symbols: and,\
#
#Naturally false elements: Empty objects, empty strings, None, and Zero
#Naturally True Statements